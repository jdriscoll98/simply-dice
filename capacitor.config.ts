import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.alexismorales.simplydice',
  appName: 'Simply Dice: Roll Anywhere',
  webDir: 'dist',
};

export default config;
